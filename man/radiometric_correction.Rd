% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/koreksi_radiometric.R
\name{radiometric_correction}
\alias{radiometric_correction}
\title{Radiometric Correction for Satellite Imagery}
\usage{
radiometric_correction(
  band,
  gain = 2e-05,
  offset = -0.1,
  sun_elevation = NULL,
  earth_sun_dist = NULL
)
}
\arguments{
\item{band}{RasterLayer object containing the satellite band data}

\item{gain}{Multiplicative rescaling factor (default values for Landsat 8)}

\item{offset}{Additive rescaling factor (default values for Landsat 8)}

\item{sun_elevation}{Sun elevation angle in degrees}

\item{earth_sun_dist}{Earth-Sun distance in astronomical units}
}
\value{
RasterLayer containing radiometrically corrected values
}
\description{
Performs radiometric correction on satellite imagery bands by converting Digital Numbers (DN)
to reflectance values. This includes both DN to radiance conversion and radiance to reflectance conversion.
}
\details{
The function applies the following corrections:
1. DN to Radiance: L = gain * DN + offset
2. Radiance to Reflectance: ρ = (π * L * d²) / (ESUN * cos(θ))
where:
- L is radiance
- d is Earth-Sun distance
- ESUN is solar irradiance
- θ is solar zenith angle (90 - sun elevation)
}
\examples{
\dontrun{
# Load a satellite band
band <- raster::raster("landsat_band.tif")

# Apply radiometric correction
corrected_band <- radiometric_correction(
  band,
  gain = 0.00002,
  offset = -0.1,
  sun_elevation = 68.5,
  earth_sun_dist = 1.014
)
}

}
